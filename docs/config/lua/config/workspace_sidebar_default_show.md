# `workspace_sidebar_default_show = true`

{{since('nightly')}}

When true, every new window opens with the
[workspace sidebar](../keyassignment/ShowWorkspaceSidebar.md) visible.
The sidebar can still be toggled with `ShowWorkspaceSidebar`.

```lua
config.workspace_sidebar_default_show = false
```
