# `file_manager_default_show = true`

{{since('nightly')}}

When true, every new tab opens with the
[file manager](../keyassignment/ShowFileManager.md) sidebar on its
right-hand side, without taking keyboard focus away from the shell.
The sidebar can still be closed and reopened with `ShowFileManager`.

```lua
config.file_manager_default_show = false
```
